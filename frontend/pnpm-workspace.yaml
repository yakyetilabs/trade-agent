# pnpm 11 records native-build-script approvals here. We depend on Firebase Auth only, so the
# Firestore/gRPC postinstall hooks are intentionally NOT run (false). Making the decision explicit
# stops pnpm from treating them as pending approvals on every install and `pnpm run`.
allowBuilds:
  "@firebase/util": false
  protobufjs: false
