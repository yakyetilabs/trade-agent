"""Operational scripts for the trade-agent backend (seed + KB ingest).

Run from ``backend/`` with the module form so ``src.`` and ``scripts.`` both
resolve, e.g. ``uv run python -m scripts.seed_firestore``.
"""
